apiVersion: v1
kind: ServiceAccount
metadata:
  name: irsa-sa
  namespace: default
  annotations:
    # Replace <AWS_ACCOUNT_ID> and <ROLE_NAME> with your actual details
    eks.amazonaws.com/role-arn: arn:aws:iam::<AWS_ACCOUNT_ID>:role/EksIrsaDynamoDbAccessRole
